# file was created

Ques : You are given a number with duplicate digits your task is to remove the immediate duplicate digits and print the result

a=list(input())
b=a.copy()
j=0
for i in range(len(a)-1):
    if a[i]==a[i+1] and j!=1:
        print(a[i])
        b.remove(a[i])
        b.remove(a[i])
        j=1
    else:
        j=0
print(*b)
